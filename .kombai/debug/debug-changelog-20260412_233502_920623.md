## Changes Made

### 1. Fixed Overlapping in Tentative Banner
- **File:** `academia-h&b/src/pages/compartidas/HorariosView_ahbb.vue`
- **Change:** Wrapped the content inside the `q-banner` for tentative starts in a `div` with left padding (`q-pl-sm`) and reduced the avatar icon size to `sm`. This prevents the icon and text from "overmounting" as seen in image-1.png.
- **Revert:** Remove the `div` and the `size="sm"` on line 348.

### 2. Standardized Header Badge for Single Class
- **File:** `academia-h&b/src/pages/compartidas/HorariosView_ahbb.vue`
- **Change:** Updated the badge color logic to use `orange` for both confirmed sessions and tentative starts (to match the orange "Clase #" style). Additionally, when a single session is found on a day, the badge now displays `Clase #X` instead of `1 Clase`, making it consistent with the card description as seen in image-2.png.
- **Revert:** Revert lines 301-314 to original logic (red badge with generic count).

### 3. Updated Database Seed Data
- **File:** `academia-backend/src/bootstrap/bootstrap.service_ahbb.ts`
- **Change:** Overhauled the base data seeding:
  - Ensured ADMIN user is `admin@academiah-b.edu` with password `admin123`.
  - Added `sembrarCursosDemo_ahbb` to create exactly 6 courses: 2 Finished (past dates), 2 Started (past/present start with students), and 2 Upcoming (future start, 0 students).
  - Automatically triggers the SQL Stored Procedure to generate coherent session dates for all demo courses.
- **Revert:** Delete the new `sembrarCursosDemo_ahbb` method and revert the single course logic in `sembrarUsuariosBase_ahbb`.

## Revert Status
- [ ] Change 1 - Banner Layout
- [ ] Change 2 - Badge Logic
- [ ] Change 3 - Base Seed Data
