# Debug Changelog - 20260409_055006_194194

## Changes Made

### 1. Updated PdfPrinter initialization in FacturasService
- **File:** `academia-backend/src/tienda/facturas.service_ahbb.ts`
- **Change:** Added imports for `fs` and `URLResolver` from `pdfmake/js/URLResolver`. Updated `PdfPrinter` constructor to pass `fs` and an instance of `URLResolver`.
- **Reason:** `pdfmake` version 0.3.x has a regression/breaking change where `PdfPrinter` crashes on the server side if `urlResolver` is not explicitly provided in the constructor, even if not using images or remote URLs.
- **Lines:** 4-10 (imports), 198-199 (instantiation)
- **Revert:** 
    - Remove `import * as fs from 'fs';` and `import URLResolver from 'pdfmake/js/URLResolver';`.
    - Change `new PdfPrinter(fonts, fs, urlResolver)` back to `new PdfPrinter(fonts)`.

## Revert Status
- [ ] Change 1 - PdfPrinter initialization
